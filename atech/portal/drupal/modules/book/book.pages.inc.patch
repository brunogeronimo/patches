--- a/book.pages.inc	2017-08-23 16:07:00.711454740 -0300
+++ b/book.pages.inc	2017-08-23 16:07:13.907510704 -0300
@@ -14,12 +14,17 @@
  * @see book_menu()
  */
 function book_render() {
-  $book_list = array();
+  $output = "";
+  //$book_list[] = l($book['title'], $book['href'], $book['options']);
   foreach (book_get_books() as $book) {
-    $book_list[] = l($book['title'], $book['href'], $book['options']);
+    $output .= '<div class="panel panel-default">'
+              .   '<div class="panel-heading first">'
+              .       '<a href="'.$book['href'].'">'.$book['title'].'</a>'
+              .   '</div>'
+              .'</div>';
   }
 
-  return theme('item_list_div', array('items' => $book_list));
+  return $output;
 }
 
 /**
